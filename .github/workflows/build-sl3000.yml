name: 🧪 测试文件复制

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 自己仓库
        uses: actions/checkout@v4

      - name: 创建 openwrt 目录
        run: mkdir -p openwrt

      - name: 列出根目录文件
        run: ls -al

      - name: 复制配置文件
        run: |
          cp -f .config diy.sh openwrt/
          cp -r files openwrt/files

      - name: 列出 openwrt 目录文件
        run: ls -al openwrt
