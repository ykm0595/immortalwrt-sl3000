name: 🚀 编译 SL-3000 ImmortalWrt 固件

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "ImmortalWrt 分支"
        required: true
        default: "openwrt-24.10"
        type: string
      multithreading:
        description: "是否开启多线程编译"
        default: true
        type: boolean
      ssh:
        description: "是否开启 SSH 调试环境"
        default: false
        type: boolean
      isFiles:
        description: "是否启用 files 大法"
        default: true
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    name: 🚀 编译 SL-3000 固件
    steps:
      - name: Checkout 自己仓库
        uses: actions/checkout@v4

      - name: Checkout ImmortalWrt 源码
        uses: actions/checkout@v4
        with:
          repository: immortalwrt/immortalwrt
          ref: ${{ inputs.branch }}
          path: openwrt

      - name: 查看磁盘空间
        run: df -hT

      - name: 优化磁盘空间
        uses: easimon/maximize-build-space@master
        with:
          root-reserve-mb: 3072
          swap-size-mb: 1
          remove-dotnet: "true"
          remove-android: "true"
          remove-haskell: "true"
          remove-codeql: "true"
          remove-docker-images: "true"

      - name: 安装编译依赖
        run: |
          sudo apt update
          sudo apt install -y \
            build-essential clang flex bison g++ gawk gcc-multilib gettext git libncurses-dev libssl-dev \
            python3 python3-pip unzip zlib1g-dev file wget rsync curl vim nano

      - name: 准备配置文件
        run: |
          mkdir -p openwrt
          cp -f .config diy.sh openwrt/
          if [ "${{ inputs.isFiles }}" = "true" ]; then
            cp -r files openwrt/files
          fi

      - name: 执行 diy.sh
        working-directory: openwrt
        run: |
          chmod +x diy.sh
          ./diy.sh

      - name: 更新 feeds
        working-directory: openwrt
        run: ./scripts/feeds update -a && ./scripts/feeds install -a

      - name: SSH 调试环境
        if: ${{ inputs.ssh == true }}
        uses: csexton/debugger-action@master

      - name: 保存编译前配置
        uses: actions/upload-artifact@v4
        with:
          name: config-before-build
          path: |
            openwrt/.config
            openwrt/feeds.conf.default
          include-hidden-files: true

      - name: 编译固件 (多线程)
        if: ${{ inputs.multithreading == true }}
        working-directory: openwrt
        run: make -j$(nproc) V=s

      - name: 编译固件 (单线程)
        if: ${{ inputs.multithreading == false }}
        working-directory: openwrt
        run: make -j1 V=s

      - name: 上传编译结果
        uses: actions/upload-artifact@v4
        with:
          name: sl3000-build-result
          path: openwrt/bin/targets
